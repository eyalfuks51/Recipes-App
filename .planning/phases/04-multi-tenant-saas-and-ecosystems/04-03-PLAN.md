---
phase: 04-multi-tenant-saas-and-ecosystems
plan: "04-03"
title: "AI Category Restrictions in Moonshot Prompt"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/moonshot.js
autonomous: true
requirements: [SAAS-04]
must_haves:
  truths:
    - "The Moonshot AI system prompt includes an explicit list of allowed categories"
    - "The AI is instructed to select only from the predefined category list (not invent new ones)"
    - "The extracted recipe's main_category field always matches one of the predefined values"
    - "Existing extractRecipeFromCaption behavior is unchanged — same return shape, same error handling"
  artifacts:
    - path: "server/src/services/moonshot.js"
      provides: "Updated system prompt with ALLOWED_CATEGORIES constant and validation"
      exports: [ALLOWED_CATEGORIES, extractRecipeFromCaption, createClient]
  key_links:
    - from: "server/src/services/moonshot.js HEBREW_SYSTEM_PROMPT"
      to: "ALLOWED_CATEGORIES array"
      via: "Template literal embeds allowed category list in the prompt string"
      pattern: "ALLOWED_CATEGORIES.join"
---

<objective>
Constrain Moonshot AI to use only a predefined set of recipe categories so the gallery can display consistent category labels.

Purpose: Without restrictions, AI invents arbitrary categories per recipe (e.g. "מרק שורשים", "תבשיל בשרי"). Predefined categories enable future filtering and ensure a consistent UI.
Output: Updated moonshot.js with ALLOWED_CATEGORIES constant and updated Hebrew system prompt that embeds the category list.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current system prompt (from server/src/services/moonshot.js):
```
'אתה עוזר לניתוח מתכונים. קרא את כיתוב האינסטגרם וחזור רק ב-JSON תקני בעברית עם השדות הבאים:
{"title": string, "main_category": string (לדוגמה: פסטה, סלט, קינוח, מרק), "difficulty": string (אחד מ: קל, בינוני, קשה), "ingredients": [מערך של שמות מרכיבים]}.
אל תוסיף טקסט מחוץ ל-JSON.'
```

Problem: `main_category` uses vague example values, not a strict enum. AI generates varied Hebrew category strings that never match.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ALLOWED_CATEGORIES and update system prompt</name>
  <files>server/src/services/moonshot.js</files>
  <action>
    In `server/src/services/moonshot.js`, make the following changes:

    1. Add `ALLOWED_CATEGORIES` constant (exported, so tests and future UI filters can reference it):
    ```js
    export const ALLOWED_CATEGORIES = [
      'פסטה',
      'סלט',
      'קינוח',
      'מרק',
      'בשר',
      'עוף',
      'דגים',
      'אורז',
      'ירקות',
      'ביצים',
      'מאפה',
      'שתייה',
      'ארוחת בוקר',
      'אחר',
    ];
    ```

    2. Replace the hardcoded `HEBREW_SYSTEM_PROMPT` string with one built from the array:
    ```js
    const HEBREW_SYSTEM_PROMPT =
      `אתה עוזר לניתוח מתכונים. קרא את כיתוב האינסטגרם וחזור רק ב-JSON תקני בעברית עם השדות הבאים: ` +
      `{"title": string, "main_category": string (חייב להיות אחד בדיוק מהרשימה: ${ALLOWED_CATEGORIES.join(', ')}), ` +
      `"difficulty": string (אחד מ: קל, בינוני, קשה), "ingredients": [מערך של שמות מרכיבים]}. ` +
      `אם הקטגוריה אינה מתאימה לאף אחת מהאפשרויות, השתמש ב"אחר". אל תוסיף טקסט מחוץ ל-JSON.`;
    ```

    3. Add a post-parse normalization step after the JSON is parsed in `extractRecipeFromCaption`. After the `if (!recipe.title ...)` check, add:
    ```js
    // Normalize category to allowed list; fallback to 'אחר'
    if (recipe.main_category && !ALLOWED_CATEGORIES.includes(recipe.main_category)) {
      recipe.main_category = 'אחר';
    }
    ```

    All other code in moonshot.js remains unchanged.
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/recepies_api/server && node --input-type=module --eval "
import { ALLOWED_CATEGORIES, extractRecipeFromCaption } from './src/services/moonshot.js';
console.log('categories:', ALLOWED_CATEGORIES.length);
console.log('includes אחר:', ALLOWED_CATEGORIES.includes('אחר'));
console.log('typeof extract:', typeof extractRecipeFromCaption);
process.exit(0);
"</automated>
  </verify>
  <done>
    ALLOWED_CATEGORIES exported with 14 Hebrew categories including 'אחר'.
    System prompt embeds the full category list using ALLOWED_CATEGORIES.join(', ').
    Normalization step maps unknown categories to 'אחר'.
    Module imports without error.
  </done>
</task>

</tasks>

<verification>
- `ALLOWED_CATEGORIES` is exported from moonshot.js
- `ALLOWED_CATEGORIES` includes 'אחר' as fallback
- System prompt string contains all category names
- Module loads without error
- extractRecipeFromCaption normalizes unknown categories to 'אחר'
</verification>

<success_criteria>
Moonshot AI receives a strict category list in its prompt. Any category not in the list is normalized to 'אחר' before saving. Gallery category badges will now display consistent Hebrew labels.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-tenant-saas-and-ecosystems/04-saas-03-SUMMARY.md`
</output>
