---
phase: 04-multi-tenant-saas-and-ecosystems
plan: "04-01"
title: "Google Auth via Supabase"
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/supabase.js
  - client/src/lib/auth.js
  - client/src/components/AuthGate.jsx
  - client/src/components/AuthGate.scss
  - client/src/App.jsx
  - client/.env.example
autonomous: true
requirements: [SAAS-01]
must_haves:
  truths:
    - "Unauthenticated users see a login screen with a 'Sign in with Google' button"
    - "Clicking the button redirects to Google OAuth via Supabase and returns the user to the app"
    - "Authenticated users see the full app (header, submit form, gallery)"
    - "A 'Sign out' button is visible when logged in and ends the session"
    - "The current user's ID is available in React context for downstream workspace scoping"
  artifacts:
    - path: "client/src/lib/auth.js"
      provides: "AuthContext, AuthProvider, useAuth hook"
      exports: [AuthProvider, useAuth]
    - path: "client/src/components/AuthGate.jsx"
      provides: "Login wall — shows Google sign-in or renders children"
      exports: [AuthGate]
    - path: "client/src/App.jsx"
      provides: "App wrapped in AuthProvider + AuthGate"
  key_links:
    - from: "client/src/App.jsx"
      to: "client/src/lib/auth.js"
      via: "AuthProvider wraps entire app; AuthGate conditionally renders content"
      pattern: "<AuthProvider>.*<AuthGate>"
    - from: "client/src/components/AuthGate.jsx"
      to: "supabase.auth.signInWithOAuth"
      via: "Google OAuth redirect on button click"
      pattern: "signInWithOAuth.*provider.*google"
---

<objective>
Add Google Authentication via Supabase OAuth so that only signed-in users can access the app.

Purpose: Phase 4 requires user identity for workspace membership. This plan creates the auth foundation all other Phase 4 plans depend on.
Output: AuthProvider context, AuthGate login wall, Google sign-in button, sign-out button wired into the app header.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Existing client stack: React 18 + Vite + SCSS + @supabase/supabase-js ^2.49.1 (already installed)
Auth flow: Supabase Google OAuth (redirect flow — not popup, better mobile/Safari support)
The app currently has no auth at all — `client/src/lib/supabase.js` creates a bare supabase client.

<interfaces>
From client/src/lib/supabase.js:
```js
import { createClient } from '@supabase/supabase-js';
export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY
);
```

Existing App.jsx structure:
- Renders <header> with brand + nav
- Renders <section class="hero"> with SubmitForm
- Renders <main class="content"> with RecipeGallery
- Uses refreshCount state + handleSuccess callback
</interfaces>

Supabase auth configuration required (human step, documented in user_setup):
- In Supabase Dashboard → Authentication → Providers → Google: enable, add Google Client ID + Secret
- In Google Cloud Console → OAuth 2.0 credentials: add Supabase callback URL
- In Supabase Dashboard → Authentication → URL Configuration: set Site URL and Redirect URLs to match Vercel domain + localhost
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthContext and auth utility</name>
  <files>client/src/lib/auth.js</files>
  <action>
    Create `client/src/lib/auth.js` exporting:
    1. `AuthContext` (React.createContext, default value `{ user: null, loading: true, signOut: () => {} }`)
    2. `AuthProvider` component that:
       - On mount, calls `supabase.auth.getSession()` to restore existing session, sets `user` from session
       - Subscribes to `supabase.auth.onAuthStateChange((_event, session) => setUser(session?.user ?? null))` — handles redirect callback automatically
       - Sets `loading = false` once session is resolved
       - Exposes `signOut` function that calls `supabase.auth.signOut()`
       - Renders `children` (no conditional rendering — AuthGate handles the gate)
    3. `useAuth()` hook: `return useContext(AuthContext)` — throws if used outside provider

    Import `supabase` from `./supabase.js`. Use `useState`, `useEffect`, `useContext`, `createContext` from react.
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/recepies_api/client && node --input-type=module --eval "import('./src/lib/auth.js').then(m => { console.log(typeof m.AuthProvider, typeof m.useAuth); process.exit(0); }).catch(e => { console.error(e.message); process.exit(1); })" 2>&1 | head -5</automated>
  </verify>
  <done>auth.js exports AuthProvider (function) and useAuth (function). AuthProvider subscribes to Supabase auth state changes and resolves session on mount.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthGate component and wire into App</name>
  <files>client/src/components/AuthGate.jsx, client/src/components/AuthGate.scss, client/src/App.jsx, client/.env.example</files>
  <action>
    **AuthGate.jsx:**
    Create component `AuthGate({ children })` that:
    - Calls `useAuth()` to get `{ user, loading, signOut }`
    - If `loading`: renders a centered full-screen spinner (single `<div className="auth-loading"><span className="spinner" /></div>`)
    - If `!user`: renders a login screen:
      ```
      <div className="auth-gate">
        <div className="auth-card">
          <BrandIcon /> (copy SVG inline from App.jsx)
          <h1>Recipe Manager</h1>
          <p>Sign in to access your workspace</p>
          <button className="btn-google" onClick={handleGoogleSignIn}>
            <GoogleIcon /> Sign in with Google
          </button>
        </div>
      </div>
      ```
      `handleGoogleSignIn` calls: `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin } })`
    - If `user`: renders `{children}` — the full app

    **AuthGate.scss:**
    Style `.auth-gate`: fixed full viewport, `background: var(--color-bg)`, flex center.
    Style `.auth-card`: white card, centered, max-width 360px, padding 40px, border-radius var(--radius-lg), shadow.
    Style `.btn-google`: full-width button, white bg, border 1px solid #e0e0e0, border-radius var(--radius-md), flex row gap-8 align-center, hover shadow lift.

    **App.jsx updates:**
    1. Import `AuthProvider` and `useAuth` from `../lib/auth`
    2. Import `AuthGate` from `./components/AuthGate`
    3. Wrap the entire return of App in `<AuthProvider><AuthGate>...existing JSX...</AuthGate></AuthProvider>`
    4. Add a sign-out button in `.site-header__inner` — call `const { user, signOut } = useAuth()` inside App (now safe since wrapped). Button: `<button className="btn-signout" onClick={signOut}>Sign out</button>` — only render if user exists.

    **client/.env.example:**
    Add line: `VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here` (it likely already exists — verify and add only if missing).
    Add comment block above auth vars:
    ```
    # Supabase Auth (Google OAuth)
    # Set up Google provider in Supabase Dashboard > Authentication > Providers
    ```
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/recepies_api/client && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npm run build` exits 0 with no errors.
    AuthGate.jsx renders login screen when no user, children when authenticated.
    App.jsx wrapped in AuthProvider + AuthGate.
    Sign-out button present in header.
  </done>
</task>

</tasks>

<verification>
- `npm run build` in client/ exits 0
- AuthGate shows login screen (not gallery) when `supabase.auth.getSession()` returns null
- Sign-in button calls `signInWithOAuth` with provider 'google'
- Sign-out button calls `supabase.auth.signOut()`
- `useAuth().user` returns the logged-in user object after OAuth callback
</verification>

<success_criteria>
Unauthenticated users see the Google sign-in card. After OAuth, they land in the full app. Signing out returns them to the login screen. The `useAuth()` hook is available for Phase 4 plans 04-04 and 04-05 to access the current user ID.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-tenant-saas-and-ecosystems/04-frontend-01-SUMMARY.md`
</output>
