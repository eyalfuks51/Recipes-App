---
phase: 11-ingredient-measurements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260314_ingredient_measurements.sql
autonomous: true
requirements:
  - DB migration for amount+unit
must_haves:
  truths:
    - "recipe_ingredients table has an `amount` TEXT column (nullable)"
    - "recipe_ingredients table has a `unit` TEXT column (nullable)"
    - "Existing rows are unaffected — amount and unit default to NULL"
    - "Migration is idempotent — running it twice produces no error"
  artifacts:
    - path: "supabase/migrations/20260314_ingredient_measurements.sql"
      provides: "SQL migration adding amount+unit to recipe_ingredients"
      contains: "ALTER TABLE recipe_ingredients"
  key_links:
    - from: "supabase/migrations/20260314_ingredient_measurements.sql"
      to: "recipe_ingredients table"
      via: "ALTER TABLE ... ADD COLUMN IF NOT EXISTS"
      pattern: "ADD COLUMN IF NOT EXISTS amount"
---

<objective>
Add `amount` and `unit` columns to the `recipe_ingredients` junction table via a Supabase SQL migration file.

Purpose: Structured ingredient measurements (quantity and unit) belong on the junction table, not the canonical `ingredients` catalog. This migration is the database foundation that all other Phase 11 plans depend on.
Output: One SQL migration file that can be pasted into Supabase Dashboard > SQL Editor and run.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ingredient-measurements/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration file 20260314_ingredient_measurements.sql</name>
  <files>supabase/migrations/20260314_ingredient_measurements.sql</files>
  <action>
Create the file `supabase/migrations/20260314_ingredient_measurements.sql` with the following exact content — do not alter the SQL:

```sql
-- Migration: 20260314_ingredient_measurements
-- Purpose: Add amount and unit columns to recipe_ingredients junction table.
--
-- Apply via: Supabase Dashboard → SQL Editor → paste contents → Run
-- This script is idempotent (IF NOT EXISTS guards).
--
-- Columns added:
--   recipe_ingredients.amount  TEXT  — quantity as-is from recipe
--                                      ("2", "קורט", "חצי", "לפי הטעם")
--   recipe_ingredients.unit    TEXT  — unit of measure
--                                      ("כוסות", "גרם", "כפות"; null if unitless)
--
-- Note: Both columns are nullable — existing rows have no measurement data
--       (NULL is the correct default for legacy rows).
--
-- Note: amount and unit belong on recipe_ingredients (the junction table),
--       NOT on the ingredients catalog table. An ingredient ("בצל") has
--       different amounts in different recipes.

ALTER TABLE recipe_ingredients
  ADD COLUMN IF NOT EXISTS amount TEXT,
  ADD COLUMN IF NOT EXISTS unit TEXT;
```

The file header comment format must match the existing migration files (see `supabase/migrations/20260307_recipe_dimensions.sql` for reference).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const sql = fs.readFileSync('supabase/migrations/20260314_ingredient_measurements.sql', 'utf8'); if (!sql.includes('ADD COLUMN IF NOT EXISTS amount TEXT')) throw new Error('amount column missing'); if (!sql.includes('ADD COLUMN IF NOT EXISTS unit TEXT')) throw new Error('unit column missing'); console.log('Migration file OK');"</automated>
  </verify>
  <done>File exists at `supabase/migrations/20260314_ingredient_measurements.sql`. Contains `ALTER TABLE recipe_ingredients` with `ADD COLUMN IF NOT EXISTS amount TEXT` and `ADD COLUMN IF NOT EXISTS unit TEXT`. Both columns nullable, migration is idempotent.</done>
</task>

</tasks>

<verification>
After creating the file:
1. Confirm the file exists: `ls supabase/migrations/20260314_ingredient_measurements.sql`
2. Confirm it contains both column additions: search for `ADD COLUMN IF NOT EXISTS amount` and `ADD COLUMN IF NOT EXISTS unit`
3. Confirm neither column targets the `ingredients` table — the ALTER TABLE line must reference `recipe_ingredients`

The migration is NOT applied to the database in this plan — it must be run manually via Supabase Dashboard > SQL Editor. Note this clearly in the SUMMARY so the executor of Plan 11-03 knows whether the DB has been updated.
</verification>

<success_criteria>
`supabase/migrations/20260314_ingredient_measurements.sql` exists, is syntactically valid SQL, and contains exactly the two column additions on `recipe_ingredients` with `IF NOT EXISTS` guards.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ingredient-measurements/11-01-SUMMARY.md` recording:
- The migration file path and its content
- Whether the migration has been applied to the Supabase database (include a note asking the user to apply it via Dashboard if not yet done)
- Any observations about existing `recipe_ingredients` structure
</output>
