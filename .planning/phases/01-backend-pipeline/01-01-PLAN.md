---
phase: 01-backend-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/package.json
  - server/src/index.js
  - server/.env.example
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Express server starts and responds to requests on the configured PORT"
    - "CORS allows requests from any origin (wildcard for v1 personal tool)"
    - "Environment variables APIFY_TOKEN, MOONSHOT_API_KEY, SUPABASE_URL, SUPABASE_KEY are read from .env"
    - ".env.example documents all required variables"
  artifacts:
    - path: "server/src/index.js"
      provides: "Express app entry point with CORS and PORT config"
      exports: ["app"]
    - path: "server/package.json"
      provides: "Node.js project manifest with express and cors dependencies"
      contains: "express"
    - path: "server/.env.example"
      provides: "Environment variable documentation"
      contains: "APIFY_TOKEN"
  key_links:
    - from: "server/src/index.js"
      to: "process.env.PORT"
      via: "app.listen(process.env.PORT || 3000)"
      pattern: "process\\.env\\.PORT"
    - from: "server/src/index.js"
      to: "cors middleware"
      via: "app.use(cors())"
      pattern: "app\\.use\\(cors"
---

<objective>
Scaffold the Express server with CORS, configurable PORT, and environment variable wiring.

Purpose: Every subsequent plan builds on this foundation — the Express app, env loading, and project structure must exist before adding routes or services.
Output: A runnable Express server at server/src/index.js that reads env vars and accepts cross-origin requests.
</objective>

<execution_context>
@C:/Users/Eyal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eyal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize server package and install dependencies</name>
  <files>server/package.json</files>
  <action>
    Create the server/ directory and initialize a Node.js project. Install dependencies:
    - express (HTTP server)
    - cors (CORS middleware)
    - dotenv (env var loading)
    - @supabase/supabase-js (Supabase client — needed by Plan 03)
    - openai (Moonshot AI uses OpenAI-compatible API — needed by Plan 02)

    Set "type": "module" in package.json so ES module imports work.
    Add a "start" script: "node src/index.js".

    Run: mkdir -p server/src && cd server && npm init -y && npm install express cors dotenv @supabase/supabase-js openai

    Then edit package.json to add "type": "module" and the start script.
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/recepies_api/server && node -e "import('express').then(() => console.log('OK'))"</automated>
  </verify>
  <done>server/package.json exists with express, cors, dotenv, @supabase/supabase-js, openai listed in dependencies, and "type": "module" set</done>
</task>

<task type="auto">
  <name>Task 2: Create Express entry point with CORS, PORT, and env wiring</name>
  <files>server/src/index.js, server/.env.example</files>
  <action>
    Create server/src/index.js:

    ```javascript
    import 'dotenv/config';
    import express from 'express';
    import cors from 'cors';

    const app = express();
    const PORT = process.env.PORT || 3000;

    app.use(cors());
    app.use(express.json());

    app.get('/health', (req, res) => {
      res.json({ status: 'ok' });
    });

    // Recipe routes will be mounted here by Plan 04
    // import recipeRouter from './routes/recipe.js';
    // app.use('/api', recipeRouter);

    app.listen(PORT, () => {
      console.log(`Server running on port ${PORT}`);
    });

    export { app };
    ```

    Create server/.env.example:
    ```
    PORT=3000
    APIFY_TOKEN=your_apify_token_here
    MOONSHOT_API_KEY=your_moonshot_api_key_here
    SUPABASE_URL=https://your-project.supabase.co
    SUPABASE_KEY=your_supabase_anon_key_here
    ```

    Note: Do NOT create a real .env file — that is the user's responsibility with real credentials.
  </action>
  <verify>
    <automated>cd C:/dev/github/personal/recepies_api/server && node src/index.js &amp; sleep 2 &amp;&amp; curl -s http://localhost:3000/health &amp;&amp; kill %1</automated>
  </verify>
  <done>GET /health returns {"status":"ok"} and server logs "Server running on port 3000"</done>
</task>

</tasks>

<verification>
From server/ directory:
1. `node src/index.js` starts without errors
2. `curl http://localhost:3000/health` returns `{"status":"ok"}`
3. server/.env.example contains PORT, APIFY_TOKEN, MOONSHOT_API_KEY, SUPABASE_URL, SUPABASE_KEY
</verification>

<success_criteria>
- server/package.json has "type":"module" and all five dependencies installed
- server/src/index.js runs and serves /health
- server/.env.example documents all five required environment variables
- CORS middleware is active (any origin allowed for v1 personal tool)
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-pipeline/01-01-SUMMARY.md`
</output>
